# Codex profile: local implementation

Use repository discovery first, then perform bounded local changes that are authorized by the current task. Use `.agents/skills/validation-reporting/SKILL.md` for validation summaries. This profile does not grant remote-action authority.
