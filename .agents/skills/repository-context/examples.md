# Repository Context Skill Examples

## Read-only discovery

- Confirm the requested revision with `git rev-parse HEAD`.
- Inspect workspace state with `git status --short`.
- Read root `AGENTS.md`, applicable nested `AGENTS.md`, relevant stable reference pages, and relevant accepted ADRs.
- Report whether implementation can proceed locally.
