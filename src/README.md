# Source layout

`src/pages/` contains Astro route entry points for the published Pattrn Pages site.

The canonical project documentation remains in `docs/**`.

Most site implementation code lives under `src/site/**` so route files stay separate from layouts, components, styles, and docs-rendering helpers.
